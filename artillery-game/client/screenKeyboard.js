
screenKeyboard = function (game)
{
    //game = game;
    //displayText = game.add.text(game.width / 2, 0, this.text, {fontSize: '32px', fill: '#000'});
    //
    ////here you define what happens if any key is pressed, default is null
    //game.input.keyboard.onDownCallback = function(e) {
    //    //for demonstration, next line prints the keyCode to console
    //    console.log(e.keyCode.toString());
    //    displayText.text += String.fromCharCode(e.keyCode);
    //}

    //window.prompt("Enter your name", "Steve");
}